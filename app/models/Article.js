class Article {
    constructor(titleText, bodyText, pub, featured) {
        this.title = titleText;
        this.body = bodyText;
        this.public = pub;
        this.featured = featured;
        this.tag = [];
    }
}